name: 'dwh'
version: '0.1'
profile: 'jaffle_shop'
config-version: 2

model-paths: ["models"]
seed-paths: ["seeds"]
test-paths: ["tests"]
analysis-paths: ["analysis"]
macro-paths: ["macros"]

target-path: "target"
clean-targets:
    - "target"
    - "dbt_modules"
    - "logs"

require-dbt-version: [">=0.21.1"]

models:
  dwh:
      materialized: table
      staging:
        materialized: view
